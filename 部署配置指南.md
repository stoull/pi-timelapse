# 时光延时 · 部署配置指南

按本文可以从硬件选型做到浏览器能打开控制台并拍出第一张照片。目标环境是 **Raspberry Pi OS + Camera Module 3**，服务默认监听 **8080**。运行方式二选一：宿主机 Python + systemd，或 Docker Compose（第 6.1 节）。

更完整的功能说明见 [项目说明文档.md](项目说明文档.md)。架构见 [docs/02-技术实现方案.md](docs/02-技术实现方案.md)。

---

## 1. 硬件要求

### 必选

| 部件 | 要求 | 说明 |
| --- | --- | --- |
| 主机 | **Raspberry Pi 4（建议 4 GB 及以上）或 Pi 5** | 拍照、实时预览和 ffmpeg 编码都在本机。内存过小容易在 4K 预览/导出时吃紧。 |
| 系统存储 | 高质量 microSD（建议 32 GB 及以上，A2） | 只放系统和本服务，**不要把大量原图写在 SD 卡上**。 |
| 相机 | **树莓派官方 Camera Module 3**（Standard 约 66° 或 Wide 约 120°） | 传感器 IMX708，1200 万像素，相位对焦。本软件按该模组编写，**不支持 USB 网络摄像头、旧版 v1/v2 模组作为正式方案**。 |
| 排线 | 与主板匹配的官方 CSI 排线 | **Pi 4：15 针 CSI。Pi 5：22 针 CAM/DISP，需 Pi 5 专用相机排线或转接。** 插反、未锁扣是「检测不到相机」的最常见原因。 |
| 电源 | 官方电源（Pi 4 约 5V/3A，Pi 5 用官方 27W 套装） | 欠压会导致随机重启、相机初始化失败、SD 损坏。 |

### 强烈建议

| 部件 | 要求 | 说明 |
| --- | --- | --- |
| 素材盘 | USB 3 SSD 或高质量移动硬盘，**50 GB 起，长期项目 500 GB～数 TB** | 延时是连续写 JPEG。4K、数秒一拍，一天可以到几十 GB。 |
| 固定 | 金属云台 / 三脚架 + 防松 | 线缆自重会慢慢把构图拉歪。 |
| 网络 | 网线或稳定 Wi-Fi；能解析本机主机名更佳 | 控制台走局域网。长期拍摄用有线更稳，也利于 NTP 校时。 |
| 散热 | 壳体通风或官方散热 | 预览和 4K 连拍会发热；过热会降频。 |

### 按场景选配

- **户外天空**：防水壳、光学玻璃窗、遮阳、防结露；模组本身不防水。  
- **长期无人值守**：UPS；完全离线时加 **RTC（如 DS3231）**，否则停电后文件名时间会错。  
- **倒装机位**：软件支持 **180° 旋转**，不需要在驱动里翻转传感器。  
- **Pi 5 + Module 3**：确认买的是 Pi 5 相机排线，不要拿 Pi 4 的 15 针线硬插。

本仓库当前现场示例：主机名 `hutpi.local`，应用目录 `/opt/pi-timelapse`，浏览器 `http://hutpi.local:8080/`。你的环境把主机名和路径换成自己的即可。

---

## 2. 系统准备

1. 刷入 **Raspberry Pi OS Bookworm 64-bit**（桌面或 Lite 均可；无头长期拍摄用 Lite 更省）。  
2. 首次开机启用 SSH，设好时区（方案默认 `Asia/Shanghai`）、主机名、Wi-Fi 或网线。  
3. **排线断电插拔**：金属面接排针、锁扣扣紧；Pi 5 注意 CAM 口编号与排线方向。  
4. 更新并安装相机栈、ffmpeg、Python 虚拟环境：

```bash
sudo apt update
sudo apt install -y python3-picamera2 python3-libcamera python3-venv ffmpeg git
```

5. 确认模组被 libcamera 看到（应出现 **imx708** 一类信息）：

```bash
rpicam-hello --list-cameras
```

若为空：断电重插排线、换线、确认不是 USB 相机、`groups` 里当前用户属于 `video`。Bookworm 走 libcamera，一般不必再开旧的 `raspistill` 接口。

6. 运行用户需要能访问相机。官方镜像用户 `pi` 通常已在 `video` 组；若自建系统用户：

```bash
sudo usermod -aG video "$USER"
```

重新登录后生效。

---

## 3. 安装应用

Picamera2 **必须用系统 apt 包**，不要用 pip 再装一套 libcamera。虚拟环境务必加 `--system-site-packages`。

```bash
sudo mkdir -p /opt/pi-timelapse
sudo chown -R "$USER":"$USER" /opt/pi-timelapse

# 把本仓库拷到 /opt/pi-timelapse
# 示例：git clone <仓库地址> /opt/pi-timelapse
# 或从开发机 rsync（见第 8 节）

cd /opt/pi-timelapse
python3 -m venv --system-site-packages .venv
.venv/bin/pip install --upgrade pip
.venv/bin/pip install -e .
.venv/bin/timelapse check-camera
```

`check-camera` 应打印 Picamera2 枚举到的相机信息。失败则先解决第 2 节检测问题，再装服务。

Python 依赖见 `pyproject.toml`（FastAPI、uvicorn、Pydantic、Pillow、PyYAML、astral 等）。**不要**在 venv 里 pip 安装 `picamera2`。

---

## 4. 素材盘与目录权限

推荐用 **UUID 固定挂载**，避免 `/dev/sda1` 变来变去。

```bash
lsblk -f
sudo mkdir -p /mnt/ssd
```

在 `/etc/fstab` 增加一行（把 UUID 换成你的，文件系统按实际改）：

```text
UUID=你的UUID  /mnt/ssd  ext4  defaults,noatime,nofail  0  2
```

```bash
sudo mount -a
df -h /mnt/ssd
```

`nofail` 可避免盘未接上时系统卡在启动；盘不在时拍摄会因空间/路径失败，属预期。

把素材目录交给运行服务的用户（本仓库 unit 默认是 `pi`）：

```bash
sudo mkdir -p /mnt/ssd/timelapse
sudo chown -R pi:pi /mnt/ssd/timelapse
```

若素材在 `/media/某用户/...` 这类自动挂载点，必须保证 **服务用户对该路径可读写**，且挂载早于 `timelapse` 启动（可在 unit 里加 `After=` 或 `RequiresMountsFor=`）。

Web 里创建项目时填写绝对路径，例如 `/mnt/ssd/timelapse`。禁止用 `/`、`/etc`、`/usr`、`/boot`。

每个项目落盘结构：

```text
<存储根>/<project_id>/
  project.yaml
  state.json
  frames/YYYY-MM-DD/frame_YYYYMMDD_HHMMSS_mmm.jpg
  previews/
  thumbnails/
  exports/
  logs/
```

项目注册表（当前激活项目、各项目根路径）在状态目录，**不是**照片目录：

- 生产默认：`/var/lib/pi-timelapse`  
- 手动试跑可改 `--state-dir`

```bash
sudo mkdir -p /var/lib/pi-timelapse
sudo chown pi:pi /var/lib/pi-timelapse
```

---

## 5. 先手动启动一次

确认相机和依赖无误后再装 systemd：

```bash
cd /opt/pi-timelapse
.venv/bin/timelapse serve --host 0.0.0.0 --port 8080 --state-dir /var/lib/pi-timelapse
```

同一局域网浏览器打开：

- `http://<树莓派IP>:8080/`  
- 若配置了 mDNS：`http://<主机名>.local:8080/`

本机查 IP：`hostname -I`。

看到「时光延时」即可 Ctrl+C 停掉前台进程，改用下面的开机服务。

---

## 6. systemd 开机自启

仓库已提供 `systemd/timelapse.service`，要点：

- 用户 / 组：`pi`（并加入 `video`）  
- 工作目录：`/opt/pi-timelapse`  
- 启动：`.venv/bin/timelapse serve --host 0.0.0.0 --port 8080 --state-dir /var/lib/pi-timelapse`  
- 环境变量：`TIMELAPSE_APP_ROOT=/opt/pi-timelapse`（用来找 `config/presets`）  
- 崩溃自动重启  

若你的登录用户不是 `pi`，先改 unit 里的 `User=` / `Group=`，并 chown 应用目录、状态目录和素材盘。

```bash
sudo cp /opt/pi-timelapse/systemd/timelapse.service /etc/systemd/system/timelapse.service
sudo chown -R pi:pi /opt/pi-timelapse /var/lib/pi-timelapse
sudo systemctl daemon-reload
sudo systemctl enable --now timelapse
systemctl is-active timelapse
sudo systemctl status timelapse --no-pager
```

日志：

```bash
journalctl -u timelapse -f
```

防火墙若开启，放行 8080（仅局域网）。**不要做路由器端口转发。** 远程请用 Tailscale、WireGuard 等。

原生 `timelapse.service` 与 Docker 版 `timelapse-docker.service` 互斥（都会占相机和 8080）。启用其中一个即可。

---

## 6.1 改用 Docker（可选）

适合已经安装 Docker、希望用镜像固化系统包与 ffmpeg 的环境。相机仍是宿主机上的 CSI 模组，容器需要访问 `/dev` 与 udev，因此 compose 使用 **privileged**，只应跑在自己的树莓派上。

镜像基于 Debian Bookworm，并加入 Raspberry Pi 软件源以安装 **python3-picamera2**。必须在 **linux/arm64**（64-bit Raspberry Pi OS）上构建，不要在 Intel/Apple 电脑上当生产镜像来构建（源里的相机包是 ARM 的）。

### 安装 Docker

64-bit 树莓派上可用发行版软件包（需带 Compose v2：`docker compose`）：

```bash
sudo apt update
sudo apt install -y docker.io
sudo apt install -y docker-compose-v2 || sudo apt install -y docker-compose-plugin || sudo apt install -y docker-compose
sudo usermod -aG docker "$USER"
```

重新登录后再用 `docker`。确认：

```bash
docker version
docker compose version
```

宿主机仍建议先能 `rpicam-hello --list-cameras`，再启动容器。

### 启动

与 systemd 原生服务不要同时开：

```bash
sudo systemctl stop timelapse
sudo systemctl disable timelapse

cd /opt/pi-timelapse
cp -n .env.example .env
docker compose up -d --build
docker compose ps
docker compose exec timelapse timelapse check-camera
```

浏览器仍访问 `http://<树莓派IP>:8080/`。日志：

```bash
docker compose logs -f timelapse
```

更新代码后在同一目录再执行 `docker compose up -d --build`。

开机自启可用仓库里的 Docker unit（会与原生 `timelapse.service` 冲突，不能一起 enable）：

```bash
sudo cp /opt/pi-timelapse/systemd/timelapse-docker.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now timelapse-docker
```

### 路径与素材盘

容器内与原生安装对齐：`TIMELAPSE_APP_ROOT=/opt/pi-timelapse`，状态目录 `/var/lib/pi-timelapse`（默认绑定到宿主机同一路径，原有项目注册表可以接着用）。

compose 把宿主机的 **`/mnt` 和 `/media` 按原路径挂进容器**。Web 里创建项目时填写的绝对路径必须落在这些挂载里，例如 `/mnt/ssd/timelapse`。若素材在别的盘符，增加 `docker-compose.override.yml`：

```yaml
services:
  timelapse:
    volumes:
      - /srv/timelapse:/srv/timelapse
```

然后在网页里把存储位置写成 `/srv/timelapse`。

### 切回原生 systemd

```bash
sudo systemctl disable --now timelapse-docker
cd /opt/pi-timelapse && docker compose down
sudo systemctl enable --now timelapse
```

---

## 7. 第一次使用检查清单

1. 浏览器打开控制台（改过前端文件时用强制刷新）。  
2. **新建项目** → 选方案 → 名称、项目 ID、存储绝对路径。  
3. 首页应出现实时画面；倒装则在「相机设置」选旋转 180° 并写入设置。  
4. 「相机设置」里测试拍摄、单次自动对焦，确认构图与锐度。  
5. 点「开始拍摄」，过一个间隔后「已拍帧数」增加，「最新拍摄」有缩略图。  
6. 拍十几张后「生成视频」，确认本机已装 ffmpeg，下载 MP4 能播放。  
7. 重启服务或重启树莓派：`sudo systemctl restart timelapse`，确认正在拍的项目会尝试恢复。

---

## 8. 从开发机更新代码（已有安装）

不要把开发机的 `.venv` 同步到树莓派，也不要用 `rsync --delete` 删掉派上的 `/opt/pi-timelapse/.venv`。

示例（把路径和主机名换成你的）：

```bash
rsync -az \
  --exclude '.venv' \
  --exclude '.pytest_cache' \
  --exclude '__pycache__' \
  --exclude '.dev-state' \
  --exclude '*.egg-info' \
  --exclude '.DS_Store' \
  -e 'ssh -o BatchMode=yes' \
  "/本地/Raspberrypi延时摄影项目/" \
  pi@树莓派主机:/tmp/pi-timelapse-src/ && \
ssh -o BatchMode=yes pi@树莓派主机 \
  'sudo rsync -a --delete --exclude ".venv" /tmp/pi-timelapse-src/ /opt/pi-timelapse/ && \
   sudo systemctl restart timelapse && sleep 2 && systemctl is-active timelapse'
```

更新代码后：原生安装执行上面的 rsync 并 `systemctl restart timelapse`；Docker 安装 rsync 到 `/opt/pi-timelapse` 后执行 `docker compose up -d --build`（或 `systemctl restart timelapse-docker`）。不要把开发机 `.venv` 打进镜像（`.dockerignore` 已排除）。

若提示找不到 `.venv/bin/timelapse`，在 **树莓派上** 重建环境后再重启：

```bash
cd /opt/pi-timelapse
python3 -m venv --system-site-packages .venv
.venv/bin/pip install -e .
sudo systemctl restart timelapse
```

改过 `pyproject.toml` 依赖时，在派上执行 `.venv/bin/pip install -e .`。只改 Python/网页文件时，同步并 `systemctl restart` 即可。

---

## 9. 常用配置项

| 项 | 位置 | 含义 |
| --- | --- | --- |
| 监听地址/端口 | `timelapse serve --host --port`，或环境变量 `TIMELAPSE_HOST` / `TIMELAPSE_PORT` | 默认 `0.0.0.0:8080`；Docker 见 `.env` |
| 状态目录 | `--state-dir` / `TIMELAPSE_STATE_DIR` | 激活项目、项目根路径注册表；Docker 默认同宿主机 bind |
| 运行方式 | `systemd/timelapse.service` 或 `docker compose` | 二者互斥 |
| 应用根 | `TIMELAPSE_APP_ROOT` | 默认用于 `config/presets` |
| 预设目录 | `TIMELAPSE_PRESETS` | 可覆盖预设路径 |
| 拍摄间隔、窗口、相机 | 各项目 `project.yaml` 或网页 | 相机参数属于当前项目 |
| 最低剩余空间 | 创建项目时 / `storage.min_free_gb` | 默认 5 GB |
| 导出帧率 | 网页导出下拉 | 24 / 25 / 30，写入该次 ffmpeg |

时区、日出日落窗口里的经纬度来自方案 YAML；换城市请改预设或项目详情/配置中的窗口字段。

---

## 10. 故障排除

| 现象 | 处理 |
| --- | --- |
| `rpicam-hello` 无相机 | 断电重插排线、Pi 4/Pi 5 线材是否匹配、模组是否 Module 3 |
| `Camera __init__ sequence did not complete` | 确认没有第二个进程占用相机；停掉手动 `rpicam-*`；重启 `timelapse`；开拍前若刚开着预览，服务会停相机并稍等再启动 |
| 服务 inactive / 找不到 `timelapse` 命令 | 确认 `/opt/pi-timelapse/.venv` 用 `--system-site-packages` 创建且 `pip install -e .` |
| 导入 picamera2 失败 | 不要纯 pip 环境；必须能 `python3 -c "from picamera2 import Picamera2"`（系统包） |
| 导出失败「未安装 ffmpeg」 | `sudo apt install ffmpeg` |
| 创建项目提示路径不可写 | `chown` 服务用户、检查挂载、不要用系统关键目录 |
| 网页是旧界面 | 强制刷新（如 Cmd+Shift+R） |
| 开机拍不了盘 | `fstab` 是否挂上；`journalctl -u timelapse` 看权限与空间 |
| 权限 denied 访问 `/dev/video*` | 用户加入 `video` 组并重新登录/重启服务 |
| Docker 构建失败（picamera2 / raspi 源） | 必须在 64-bit ARM 树莓派上 `docker compose build`，不要用 amd64 电脑打生产镜像 |
| Docker 内检测不到相机 | 停掉宿主机 `timelapse` 与 `rpicam-*`；确认 compose 为 privileged 且挂了 `/dev`、`/run/udev`；`docker compose exec timelapse timelapse check-camera` |
| Docker 与 systemd 抢 8080 | `sudo systemctl stop timelapse` 或 `docker compose down`，只留一种 |
| 容器内存储路径不存在 | 把宿主机目录 bind 到容器内相同路径；默认已挂 `/mnt` 与 `/media` |

开发机（无相机）可测逻辑：

```bash
cd /opt/pi-timelapse   # 或本地仓库
python3 -m venv .venv && .venv/bin/pip install -e '.[test]'
.venv/bin/pytest
```

无 Picamera2 时不要在开发机上跑 `check-camera` 当真机验收。

---

## 11. 安全与运维注意

- 控制台无账号密码，等同于局域网里谁都能开拍、删图、导出。  
- 长期拍摄优先有线供电和有线网络；植物/天空项目把 NTP 或 RTC 当作必选项。  
- 根分区与素材分离；定期看盘容量和 `journalctl -u timelapse`（Docker 则看 `docker compose logs`）。  
- Docker 方式因访问相机使用 privileged，不要把该容器暴露到公网。

按第 2～7 节做完原生部署；第 6.1 节为可选的 Docker 部署。
