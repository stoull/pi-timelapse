# 时光延时 · 项目说明

这是一套跑在树莓派上的**单机延时摄影系统**，用官方 Camera Module 3 按固定间隔拍 JPEG，用局域网浏览器控制拍摄，再把照片合成 H.264 MP4。界面名称是「时光延时」。

适合长期拍天空、植物生长、室内起居、街道人流等；不适合当监控录像机、也不做公网直播。

---

## 它解决什么问题

延时摄影最怕的不是「某一张不好看」，而是**帧与帧之间不一致**：自动曝光乱跳、白平衡乱飘、对焦来回呼吸、漏拍后再补拍把节奏打乱。

本项目把一次拍摄做成一个**项目**：间隔、分辨率、拍摄窗口、对焦/曝光/白平衡都记在该项目里。同一台树莓派一次只开一个项目、只占用一颗相机。

---

## 谁来用、怎么用

同一局域网里用手机或电脑打开控制台（默认 `http://树莓派地址:8080`），即可：

1. 选一套拍摄方案，创建项目并指定素材存放目录  
2. 看实时画面，调相机参数，必要时旋转 180°（倒装）  
3. 立刻开拍，或预约开始/结束时间  
4. 暂停、继续、停止；需要时清空本项目照片后重拍  
5. 浏览、筛选、删除照片  
6. 选择 24 / 25 / 30 fps，可选水印，后台导出 MP4 并下载  

开发机可以跑配置和调度测试，**真正拍照必须在接好模组的树莓派上**。

---

## 主要功能

### 项目与方案

- **15 套内置方案**（YAML），创建项目时复制进该项目的相机与拍摄配置，之后可再改。
- 三类题材：`sky` 天空、`grow` 植物、`life` 日常/街景。
- 每个项目有独立目录：配置、按日存放的原图、缩略图、导出视频、日志。
- 可建多个项目并切换；切换前会停掉当前拍摄，不会两路抢同一颗相机。
- 项目列表用最早一帧做小封面；详情里可改间隔和拍摄窗口。

| 方案 ID | 名称 | 典型间隔 | 用途一句话 |
| --- | --- | --- | --- |
| `sky-cloud` | 天空云层 | 8 秒 | 白天流云 |
| `sky-golden` | 天空黄金时段 | 5 秒 | 日出前后暖光 |
| `sky-sunset` | 日落晚霞 | 4 秒 | 日落色彩 |
| `sky-storm` | 风暴云层 | 3 秒 | 积云与天气快速变化 |
| `sky-night` | 星空夜景 | 25 秒 | 锁定长快门与增益 |
| `grow-indoor` | 室内植物 | 15 分钟 | 窗边或灯光下盆栽 |
| `grow-season` | 生长季 | 45 分钟 | 户外植物跨日 |
| `grow-bloom` | 花朵绽放 | 10 分钟 | 近距开花 |
| `life-activity` | 室内活动 | 2 秒 | 做饭、聚会等短时过程 |
| `life-room-day` | 起居室一天 | 10 秒 | 室内日光变化 |
| `life-week` | 一周起居 | 30 秒 | 更长周期、更省存储 |
| `life-city-night` | 城市夜景 | 8 秒 | 灯火与天际线 |
| `life-traffic` | 车流光轨 | 3 秒 | 夜拍车灯轨迹 |
| `life-construction` | 工地建设 | 3 分钟 | 工作时段进度 |
| `life-street` | 街道人流 | 2 秒 | 路口或广场 |

创建时可覆盖间隔、分辨率、钟点窗口和最低剩余空间。存储路径必须是树莓派上的**绝对路径**，项目建好后不会自动搬家。

### 拍摄控制

- **开始 / 暂停 / 继续 / 停止**。暂停后再继续**不会补拍**错过的时间槽，时间轴保持真实间隔。
- **定时拍摄**：预约开始时间；可选自动结束。树莓派重启后仍会尝试恢复预约。
- **拍摄窗口**：全天、按钟点、按日出日落（需经纬度）、或手动事件型窗口。窗口外进入等待，不拍空镜。
- **清空并重新开始**：删掉该项目已拍照片/预览/导出后重新开拍（配置保留）。
- 空闲空间低于项目阈值（默认约 5 GB）会停拍，避免把盘写满。
- 服务崩溃或重启时，若上次正在拍，会尝试按状态文件恢复拍摄。

### 相机与实时画面

针对 **Raspberry Pi Camera Module 3（IMX708）**，经 Picamera2 / libcamera 控制。光圈是模组固定的，软件里不能调。

未拍摄时，首页轮询**实时 JPEG 预览**（相机会保持开启）。正在拍摄时，预览改为**最新已保存的那一张**，避免和连拍抢相机。

「相机设置」只作用于**当前项目**，且仅在未拍摄时可打开：

- 对焦：单次自动对焦或手动屈光度；可点「单次自动对焦」后锁定  
- 曝光：自动曝光或锁定快门/增益；曝光补偿  
- 白平衡：可关自动并锁红色/蓝色增益或预设  
- 亮度、对比度、饱和度、锐度、HDR  
- 照片分辨率：1920×1080、2304×1296、3840×2160、4608×2592  
- JPEG 质量  
- **画面旋转**：不旋转，或 **180°**（倒装）；会同时作用在预览和成片。不提供 90°/270°  

拖动参数会立刻打到相机并刷新设置页预览；满意后再写入当前项目。首页「实时画面」旁会显示未旋转或已旋转 180°。

### 运行概览

控制台显示已拍帧数、间隔、拍摄窗口、最后拍摄时间、素材盘剩余空间、CPU、温度、内存，以及最新一张照片的缩略图。

### 照片与成片

- 相册可按时间间隔或自定义起止筛选，分页加载，可单张或批量删除。  
- **生成视频**：用 ffmpeg 把该项目全部 JPEG 合成 H.264 MP4（`libx264`，可快开播放）。  
- 导出帧率可选 **24 / 25 / 30 fps**（concat 时长和 ffmpeg `-r` 一致）。  
- 可选水印：无、时间戳（日期时间或仅时刻）、自定义文本（最多 50 字）；位置七档。  
- 导出在后台进行，完成后可下载；历史导出可在项目详情中查看。

成片高度等参数来自项目配置；原图始终保留在项目目录，视频只是导出物。

---

## 使用上建议记住的几点

1. **先构图、再锁对焦和白平衡，再长时间开拍。** 中途大幅改构图，等于换了一个项目。  
2. 间隔按成片来反推：`成片秒数 ≈ 总张数 / 播放帧率`，`总张数 ≈ 拍摄时长 / 间隔`。  
3. 4K、短间隔会很快占满磁盘。日常长拍更常用 1080p 或 2.3K、更长间隔。  
4. 默认只监听局域网，**没有登录认证**。不要把 8080 端口映射到公网。  
5. 实时预览会让模组保持工作、略发热；开拍前系统会先释放相机再启动，避免初始化失败。

---

## 软件结构（便于对照代码）

| 部分 | 作用 |
| --- | --- |
| `src/timelapse/web/` | FastAPI 控制台与 API |
| `src/timelapse/runtime.py` | 拍摄循环、窗口、定时、预览与调参互斥 |
| `src/timelapse/camera/` | Picamera2 适配与可调参数说明 |
| `src/timelapse/project.py` | 预设、项目注册表、创建/切换 |
| `src/timelapse/storage.py` | 按日写帧、空间检查、相册与导出文件 |
| `src/timelapse/encode.py` | ffmpeg 合成与水印 |
| `src/timelapse/schedule/` | 时间槽与日出日落窗口 |
| `config/presets/` | 15 套拍摄方案 |
| `systemd/timelapse.service` | 开机自启（原生） |
| `Dockerfile` / `docker-compose.yml` | 可选容器部署，与原生服务互斥 |

项目注册信息在状态目录（默认 `/var/lib/pi-timelapse`）；照片在你为该项目指定的存储根目录下。

Python 包名 `pi-timelapse`（见 `pyproject.toml`）。当前版本将拍摄、预览、调参、相册和导出都放在同一 Web 服务里。

更细的拍摄原则见 [docs/01-延时摄影设计方案.md](docs/01-延时摄影设计方案.md)，与代码一致的架构见 [docs/02-技术实现方案.md](docs/02-技术实现方案.md)。从零装机见 [部署配置指南.md](部署配置指南.md)。
